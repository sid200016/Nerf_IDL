expname = flower_test
basedir = ./logs
datadir = ./data/nerf_llff_data/flower
dataset_type = llff

factor = 8
llffhold = 8

N_rand = 1024
N_samples = 64
N_importance = 64

use_viewdirs = True
raw_noise_std = 1e0

# Learnable Fourier Encoding (improves PSNR)
learnable_pe = True

# Gradient clipping for stability (recommended with learnable PE)
grad_clip = 0.5

# Weights & Biases logging (optional)
use_wandb = True
wandb_project = nerf-idl
wandb_run_name = flower_test_with fourier Encoding


